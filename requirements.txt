streamlit==1.40.1

# OpenAI Python SDK (new 1.x line – chat.completions still supported)
openai>=1.57.0

# IMAP & email parsing helpers
beautifulsoup4==4.12.3

# Optional, but useful in many Streamlit apps
python-dotenv==1.0.1
requests==2.31.0

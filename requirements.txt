streamlit==1.40.1
python-dotenv==1.0.1

openai>=1.52.0

# PDF + image processing
pymupdf==1.23.10
Pillow==10.2.0

# IMAP + email parsing
beautifulsoup4==4.12.3

# For ICS creation via text (no extra package needed, but safe)
icalendar==5.0.7
